name: Upload coverage and test results

on:
  push:
    branches:
      - main  # o la rama en la que quieras que se ejecute esta acción

jobs:
  upload-coverage:
    name: Upload coverage report to Codecov
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.13

      - name: Install dependencies
        run: pip install pytest pytest-cov codecov

      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml  # Aquí usas el archivo de cobertura generado localmente
          fail_ci_if_error: true  # Si la subida falla, también fallará el CI

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}